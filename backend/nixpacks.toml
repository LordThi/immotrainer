# Railway build config — forces rebuild
[variables]
COMPOSER_ALLOW_SUPERUSER = "1"

[phases.install]
cmds = [
  "mkdir -p /var/log/nginx && mkdir -p /var/cache/nginx",
  "echo 'APP_ENV=prod' > .env",
  "composer install --ignore-platform-reqs --no-interaction"
]
