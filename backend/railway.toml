[build]
builder = "nixpacks"

[deploy]
startCommand = "php bin/console doctrine:migrations:migrate --no-interaction && php bin/console doctrine:fixtures:load --no-interaction && php -S 0.0.0.0:$PORT -t public/"
