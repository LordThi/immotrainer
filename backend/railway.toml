[build]
builder = "nixpacks"

[deploy]
releaseCommand = "cd /app && php bin/console doctrine:migrations:migrate --no-interaction && php bin/console doctrine:fixtures:load --no-interaction"
startCommand = "php -S 0.0.0.0:$PORT -t /app/public/"
