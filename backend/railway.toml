[build]
builder = "nixpacks"

[deploy]
releaseCommand = "php bin/console doctrine:migrations:migrate --no-interaction && php bin/console doctrine:fixtures:load --no-interaction"
startCommand = "php -S 0.0.0.0:$PORT -t public/"
